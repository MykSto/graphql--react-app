import React from 'react';

const Test = (props) => {
    return(
        <div>
            <p>Title: {props.title}</p>
            <p>Text: {props.text}</p>
        </div>
    )
}

export default Test;
